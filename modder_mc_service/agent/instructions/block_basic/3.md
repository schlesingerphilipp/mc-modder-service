create the directory $MODS_FOLDER/$MOD_NAME/src/generated/resources/assets/$MOD_NAME/models/item/
in this directory create $ELEMENT_NAME.json
with the content:

```json
{
  "parent": "$MOD_NAME:block/$ELEMENT_NAME"
}
```