create the directory $MODS_FOLDER/$MOD_NAME/src/main/resources/assets/$MOD_NAME/textures/block/
copy an image from /assets to this directory with the name $ELEMENT_NAME.png with an image of choice fitting for $ELEMENT_NAME

If there is no fitting image available in /assets generate an image
