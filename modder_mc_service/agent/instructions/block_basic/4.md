create the directory $MODS_FOLDER/$MOD_NAME/src/main/resources/assets/$MOD_NAME/lang/
in this directory create or edit en_us.json
    - Add a line `"block.$MOD_NAME.$ELEMENT_NAME": "Block of $ELEMENT_NAME",` at the top.

if the file does not exist, create it